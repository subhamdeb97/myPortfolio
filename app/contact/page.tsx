'use client'
import { useState } from 'react'
export default function ContactPage(){ const [status,setStatus]=useState<string|null>(null); const submit=async(e:React.FormEvent<HTMLFormElement>)=>{ e.preventDefault(); const form=new FormData(e.currentTarget); const res=await fetch('/api/contact',{method:'POST',body:form}); setStatus(res.ok?'Thanks! I will get back to you soon.':'Something went wrong.'); e.currentTarget.reset() }
  return (<div className='max-w-xl space-y-6'><h1 className='text-2xl font-bold'>Contact</h1><form onSubmit={submit} className='card p-6 space-y-3'><input required name='name' placeholder='Your name' className='w-full px-3 py-2 rounded-xl border border-black/10 dark:border-white/10 bg-white/70 dark:bg-white/5'/><input required type='email' name='email' placeholder='Your email' className='w-full px-3 py-2 rounded-xl border border-black/10 dark:border-white/10 bg-white/70 dark:bg-white/5'/><textarea required name='message' placeholder='Your message' rows={6} className='w-full px-3 py-2 rounded-xl border border-black/10 dark:border-white/10 bg-white/70 dark:bg-white/5'/><button className='btn btn-primary'>Send</button>{status && <p className='text-sm text-gray-700 dark:text-gray-300'>{status}</p>}</form><p className='text-sm text-gray-600 dark:text-gray-400'>This is a demo endpoint. Wire it to email or a CRM later.</p></div>) }
